import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    MainViewStyle: {
        marginHorizontal: '10%',
        flex: 0.7,
        marginVertical: '5%',
    },
});

export default styles;
